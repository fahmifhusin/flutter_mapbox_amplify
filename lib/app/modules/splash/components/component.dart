part of splash_lib;

class Components extends GetView<SplashController> {
  const Components({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
