part of home_lib;

class Components extends GetView<HomeController> {
  const Components({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
